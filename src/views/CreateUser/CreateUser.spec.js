/* global describe it expect */

import React from 'react';
import CreateUserView from './CreateUser.spec.js';

describe('(view) CreateUser', function () {
  it('Should have a test that works with Chai expectations.', function () {
  });
});
