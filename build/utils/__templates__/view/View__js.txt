import styles from './${NAME}.scss';

import React from 'react';

class ${NAME}View extends React.Component {
  constructor(props) {
    super(props);
  }

  static propTypes = {
  };

  render() {
    return (
      <div>INSERT COMPONENT DATA</div>
    );
  }
}

export default ${NAME}View
