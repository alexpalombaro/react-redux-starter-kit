/* global describe it expect */

import React from 'react';
import UserEditView from './UserEdit.spec.js';

describe('(view) UserEdit', function () {
  it('Should have a test that works with Chai expectations.', function () {
  });
});
