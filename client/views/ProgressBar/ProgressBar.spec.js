/* global describe it expect */

import React from 'react';
import ProgressBarView from './ProgressBar.spec.js';

describe('(view) ProgressBar', function () {
  it('Should have a test that works with Chai expectations.', function () {
  });
});
